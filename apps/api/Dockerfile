# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @friend-workspace/api`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @friend-workspace/api -p 3000:3000`.
#
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

EXPOSE 3000

WORKDIR /app

COPY dist .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

RUN npm --omit=dev -f install

HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
  CMD node -e "fetch(`http://127.0.0.1:${process.env.PORT ?? 3000}/health`).then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"

CMD [ "node", "main.js" ]
